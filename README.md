# Probe and Solve Algorithm (PSA)
In this project, we propose a generic and non-invasive method that uses Bayesian Optimization to select effective search strategies in constraint solvers, improving performance across a wide range of problems.
